import React from 'react'

const RestaurantDetails = ({url, title, cost, rating, votes, reviews, tags}) => {
    
    return (
        <div>
            
        </div>
    )
}

export default RestaurantDetails
